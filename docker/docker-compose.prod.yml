version: "3.8"
services:
  pokerogue:
    build:
      context: ../
      dockerfile: docker/Dockerfile.prod
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
